# Army.mil Web Scraper

This Python script uses Selenium to crawl www.army.mil and search for specified keywords and phrases.

## Setup

1. Install Python dependencies:
```bash
pip install -r requirements.txt
```

2. Make sure you have Chrome browser installed

3. Update the keywords list in `config.py` as needed

4. Run the script:
```bash
python scraper.py
```
